using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gyoku : Koma
{
    
    // Start is called before the first frame update
    public Gyoku(int x, int z, int n) : base(x, z, n) { }
}
