using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Kin : Koma
{
    
    // Start is called before the first frame update
    public Kin(int x, int z, int n) : base(x, z, n) { }
}
