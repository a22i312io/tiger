using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gin : Koma
{
    
    // Start is called before the first frame update
    public Gin(int x, int z, int n) : base(x, z, n) { }
}
