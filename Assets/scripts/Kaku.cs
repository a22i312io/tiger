using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Kaku : Koma
{
   
    // Start is called before the first frame update
    public Kaku(int x, int z, int n) : base(x, z, n) { }
}
