using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Keima : Koma
{
    
    // Start is called before the first frame update
    public Keima(int x, int z, int n) : base(x, z, n) { }
}
